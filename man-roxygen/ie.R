#' @param key your SunlightLabs API key; loads from .Rprofile
#' @param page The page of results to return; defaults to 1.
#' @param per_page The number of results to return per page, defaults to 1,000. The maximum number of records per page is 100,000.
#' @param callopts Optional additional curl options (debugging tools mostly)
